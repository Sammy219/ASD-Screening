<html>
	<body>
<div class="center">
    <div class="hasil">
        <h1>Hasil Tes</h1>

        <div class="card">
            {# <div class="data"> #}
                <h3>Data Diri</h3>
                <div class="form-check">
                    <h2>Nama</h2><p>{{ bio.name }}</p>
                    <h2 class="atas">Jenis Kelamin</h2><p>{{ bio.sex }}</p>
                    {# Kelompok Etnik #}
                    <h2>Kelompok etnik</h2><p>{{ bio.ethnicity }}</p>
                    {# Penyakit Kuning #}
                    <h2>Pernah terkena penyakit kuning?</h2><p>{{ bio.jaundice }}</p>
                    {# Ada keluarga dengan ASD? #}
                    <h2 class="atas">Ada keluarga dengan ASD?</h2><p>{{ bio.family_mem_with_asd }}</p>
                    {# Kelompok assigner #}
                    <h2>Assigner</h2><p>{{ bio.assigner }}</p>
                    {# Penyakit Kuning #}
                    <h2>Umur (bulan)</h2><p>{{ bio.age }}</p>
                </div>

                <h3>Kuesioner</h3>
                <div class="form-check">
                    {% for item in qna %}
                        <h2>{{ item.q }}</h2>
                        <p>{{ item.a }}</p>
                    {% endfor %}
                </div>

                <h3>Hasil</h3>
                <div class="form-check">
                    {% if class_pred == 1 %}
                        <h2 style="color:red">Anak Anda memiliki kemungkinan {{ '%0.2f'| format(confidence|float) }}% mengalami Autism Spectrum Disorder<h2>
                        <h3>Kunjungi dokter Anda untuk diagnosis dan tindakan lebih lanjut</h3>
                    {% else %}
                        <h2 style="color:green">Anak Anda memiliki kemungkinan {{ '%0.2f'| format(confidence|float) }}% tidak mengalami Autism Spectrum Disorder<h2>
                    {% endif %}
                </div>
        </div>
    </div>
</div>
</body>
</html>